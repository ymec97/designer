# Test plan

The v0.10 test plan is consolidated in **`docs/V0.10_TEST_PLAN.md`** — it
covers the 22-item feedback batch, the merged connector-clutter changes
(relocatable anchors, caption modes + density nudge, rubber-band precision,
style-panel visibility), macOS CI, the branch topology, and the release steps.

Nothing on this branch is compile-verified (authored in Linux cloud sessions
with no Xcode). Run the release battery on a Mac before merging or tagging.
