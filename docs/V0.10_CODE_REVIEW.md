# v0.10 Compile-Risk Review — `a8fd9de..b3ab170` (feature/v0.10-feedback)

Static review of 14 commits for things a Swift 6 compiler would reject or that
are logic bugs. **No Swift toolchain in this environment** — findings are by
reading, not building. Targets: `DesignerModel` is strict Swift 6 (Sendable/
concurrency enforced); `DesignerCanvas` / `Designer` are Swift 5 + AppKit.

## Verdict

**No BLOCKERS found. Every commit appears to compile.** All identifiers,
argument labels, types, enum exhaustiveness, Codable shapes, and call-site
defaults check out against the current source. A handful of low-severity
RISK/NIT items are listed below. No test files changed in the range, so the
behavior changes in `ba8c8e6` and `23261d2` were checked against the *existing*
tests and still satisfy them.

## Findings (BLOCKERS first — there are none; then RISK, then NIT)

| Commit | File:line | Severity | Issue | Suggested fix |
|---|---|---|---|---|
| 182095f | `Designer/BoardDocument.swift:35-41` | RISK | `override var fileURL: URL?` with a `didSet` observer on an ObjC (`NSDocument`) property is valid Swift and the observer *does* fire when AppKit sets it (dynamic dispatch). But AppKit sets `fileURL` during async save/autosave on a **background thread**, and the `didSet` calls `BoardCatalog.remember(url)` which does **synchronous disk I/O** (reads `board.json`, decodes a `Board`, writes `catalog-index.json`) off the main thread on every set. Compiles; runtime thread-safety/perf concern only. | Hop `remember` to a background queue *deliberately* (or main), or debounce; confirm no re-entrancy with autosave. |
| 23261d2 | `DesignerModel/FlowRecorder.swift:97-114` | NIT | The new F12 `.both`-walked-once-each-direction path (`directedFree`) is **not exercised by any test** — `FlowTests.swift` has no `.both` edge (only `.forward`/one `.backward`). Logic reads correct; just uncovered. | Add a `.both` back-and-forth recorder test. |
| 37ed8f5 | `Designer/CommandPalette.swift:13` | NIT | `PaletteCommand.searchText` computed property is now **dead code** — scoring switched to `title`/`keywords`. Harmless (no unused-decl error for a struct member). | Delete `searchText`. |
| cdf6cd8 | `DesignerCanvas/CanvasView.swift:1064-1071` | NIT | `startScreen.x + (endScreen.x - startScreen.x) * eased` mixes `CGFloat` (from `toView -> CGPoint`) with `Double` (`eased`). Compiles via the implicit `CGFloat<->Double` conversion (SE-0307), available in Swift 5 mode. Worth an eye but not an error; `Double(screenX)` then normalizes explicitly. | None required. |
| 8186682 | `Designer/LinkedBoards.swift:358` | NIT | `NSSound.volume` is `Float`; `0.35` binds as `Float`. Fine. Commit is a *partial* F3 fix (volume only), acknowledged in its message. | None. |

## Per-commit compile check (each verified clean unless noted above)

- **48637f3 (Focus-mode / kind-dot)** — CLEAN. New `Self.dimmedFraction` (`0.22`) matches `CanvasView.dimmedAlpha` (`0.22`); `Self.dimmedLabelFloor`; static consts referenced via `Self.` inside instance `drawNode` are valid. `Self.textColor(onFill:)` is a static func (BoardRenderer.swift:444). Both `draw(...,dimmed:)` and `drawNode(...,dimmed:)` add defaulted params, so every other `renderer.draw(` call site (CanvasView 805/947/969/1322/1358/1367/1437, BoardSnapshot 55) still compiles. `image.draw(...,fraction:)` takes `CGFloat` — `dimmedFraction` is `CGFloat`. `context.saveGState()`/`restoreGState()` valid. The `pendingShapeStyle = Style()` change is consistent with `Style`'s default init.
- **0dbe835 (Shape tool sticky / outline %)** — CLEAN. `currentWidth` is `Double` (StylePanel.swift:250); `Int((currentWidth - 0.5) / (8 - 0.5) * 100)` is all-`Double` arithmetic. SwiftUI `HStack`/`Spacer`/`.monospacedDigit()` valid.
- **37ed8f5 (Command palette scoring)** — CLEAN (see NIT). `PaletteCommand.title` (:5) and `.keywords` (:10) exist; `score(query:command:)` and `tokenScore(_:in: PaletteCommand)` consistent; query `q` is lowercased (:33), so `command.title.lowercased().hasPrefix(query)` is case-consistent. `fuzzyScore(token, in: title)` unchanged signature.
- **ba8c8e6 (BoardDiff field deltas)** — CLEAN. Every `WireNode`/`WireEdge` field name+optionality in `delta`/`layersLabel` matches `WireBoard.swift:36-67` (node: `name/kind/shape/orientation/fill/stroke/opacity/layers`; edge: `direction/`\``protocol`\``/data/condition/layers`; all `String?` except `opacity: Double?`, `layers: [String]?`). `compact(_:Double)->String` is a file-scope `private func` (:258) — reachable from the `private extension` (file-private = whole-file scope; call precedes definition, which is fine). `FieldChange.init(id:before:after:)` matches the struct's synthesized memberwise init (:9-13). **Tests still pass**: the rename path uses `wire.displayName` (untouched, :140-141), satisfying the exact-string assertions in `testRenameCollapsesToOneChange`; the delta path (protocol test) is asserted only via `.contains`, so `"protocol HTTP"`/`"protocol gRPC"` still match. No lingering `.signature` refs anywhere.
- **8186682 (Swoosh volume)** — CLEAN (see NIT).
- **182095f (Catalog persisted index)** — CLEAN compile (see fileURL RISK above). `IndexRecord` (`path: String`, `boardID: String?`) gets synthesized `Codable`/`Hashable`. `BoardSerialization.board(from: data)` exists (:41, `throws`, used with `try?`). `board.id.rawValue.uuidString`: `BoardID.rawValue` is `UUID` (Identifiers.swift:8). `BoardPackage.fileExtension`/`boardFileName` exist. Single `fileURL` override, no duplicate.
- **4be528e (Hollow-shape click-through)** — CLEAN. `node.style.hasFill` (Style.swift:41) and `node.style.image` (`String?`, :21) exist; `node.semantic.name`, `Rect(x:y:width:height:)`, nested `inset` func all valid.
- **51c0521 (Block undo in read-only view)** — CLEAN. `@objc func undo(_:)`/`redo(_:)` are new methods (NSResponder declares no `undo`/`redo`), no `override` needed. `document.undoManager?.undo()/redo()/canUndo/canRedo` valid on `NSDocument`. `NSSound.beep()` valid. `case Selector(("undo:")):` in the `switch menuItem.action` (`Selector?`) is the **same pattern shape** already used by the pre-existing `case #selector(structurize(_:)):` in that switch — consistent, compiles.
- **6083a70 (Movable ink)** — CLEAN. `StrokePoint(x:y:pressure:time:)` matches the init (Geometry.swift:57). In `applyFrame(_:to element: inout Element)`, the new `case .ink(var ink):` calls `SpatialIndex.boundingRect(of: element)` then writes `element.content` — **no exclusive-access violation**: the switch reads `element.content` once and copies the associated value out; the `boundingRect` read and the later write are sequential, exactly like the pre-existing `.node`/`.note`/`.boundary` cases. The draw-loop copy (`var drawElement = element; drawElement.content = .ink(ink)`) is valid. `.edge` case still returns false, so the switch stays exhaustive.
- **1d82bc0 (Wobbly circles)** — CLEAN. `hull` is in scope (`let hull = convexHull(points)`, :80). `pathLength(_:[Point])` (:324), `polygonArea(_:[Point])` (:328), `distance(_:Point,_:Point)` (:320) all exist and take the right types; `hull[hull.count-1]`/`hull[0]` guarded by `hull.count > 1`. `hullCircularity` (:98) is in scope in the `default:` case (:137), same function.
- **932acc1 (Double-click text box, F5)** — CLEAN. `Note(text:frame:style:)` matches init (Elements.swift:232); `Element(layerIDs:sortKey:content:)` uses the defaulted init (:263); `activeLayerIDs()` returns `Set<LayerID>` matching `Element.layerIDs`; `board.topSortKey`, `creationScaleFactor()->Double`, `Style(fill: Style.noFill)`, `beginLabelEdit(for:)` all exist. `140 * factor`/`Double(size.width)` type-check.
- **23261d2 (Flow pick-a-source / bidirectional / layer filter)** — CLEAN (see F12 NIT). `DirectedKey` nested `private struct` with all-`ElementID` fields is implicitly `Sendable` (non-public struct, Sendable members) — no strict-Swift-6 issue, and it's only used in a local `Set` inside a synchronous func anyway. `journal: [Candidate]` with `.edge/.from/.to: ElementID` (:24-27) matches `DirectedKey(edge:from:to:)`. On the view side: `renderer.drawSimulationScrim(_:in:)` (BoardRenderer:958) and `drawHintCaption(_:at:in:)` (:896) signatures match the call sites; `isEditable(id:)` (CanvasView:2781), `isRecordingFlow` (:1125), `flowRecordingChanged?` closure all exist. Controller's `beginFlowSourcePick()` call resolves. Exhaustive `switch edge.semantic.direction` keeps `default:` for `.none`/unknown.
- **cdf6cd8 (Linked-board dive anchor)** — CLEAN (see CGFloat NIT). The `cameraAnimation` tuple grew to 6 labeled elements `(start,target,startTime,duration,anchorWorld: Point?, completion)`; the single construction `(viewport, target, CACurrentMediaTime(), duration, anchor, completion)` matches positionally/by-type (`anchor` is `Point?`). All `animation.<field>` reads (`.startTime`, `.duration`, `.start.scale`, `.target.scale`, `.anchorWorld`, `.start.toView`, `.start.origin.x`…) are valid. New `anchorWorld: CGPoint? = nil` param; both LinkedBoards callers pass a `CGPoint`. `toView(_ world: Point) -> CGPoint` (CanvasViewport:22) drives the anchor math; origin solve `anchor.x - Double(screenX)/scale` is dimensionally correct.
- **b3ab170 (Dangling snap on move)** — CLEAN. Combined pattern `case .insertElement(let element), .replaceElement(let element):` binds `element` — both cases are `case insertElement(Element)` / `case replaceElement(Element)` (BoardOperation.swift:9,12): identical single-`Element` associated value, so the shared binding is well-typed. `.batch(let children)` + `default` keep the switch exhaustive.
